import PropTypes from 'prop-types';

const Cart = () => {
    return (
        <>
            <div>
              
            </div>
        </>
    );
};

Cart.propTypes ={
    cart: PropTypes.object.isRequired
}
export default Cart;